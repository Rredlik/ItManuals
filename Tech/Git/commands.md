# Работа с репозиториями Github

Чтобы быстро подключиться к своему аккаунту необходимо установить программу gh
```bash
sudo apt install gh
gh auth login
```
Следуя инструкциям подключить аккаунт. Ссылка на генерацию токенов: https://github.com/settings/tokens. Добавить необходимые репозитории и права "Pull" и "**Contents → Read-only** (или Read and Write, если нужен push)"
- Если **публичный** — токен вообще не нужен, должно работать без авторизации.
- Если **приватный** — нужны именно права на **Contents → Read**.


Ввести команду, вставив нужную ссылку, в необходимой директории
```bash
git clone https://github.com/Rredlik/TgBot_EGEBot.git
```

Обновить локальный репозиторий в нужной папке ввести
```bash
git pull
```
Если не хватает прав для pull с ошибкой: remote: `Write access to repository not granted. fatal: unable to access 'https://github.com/****/****.git/': The requested URL returned error: 403`, необходимо проверить выданные права на токен

Удаление файлов из репозитория (например, файла /.env)
```bash
git rm -rf --cached /.env
```

# Ошибки и исключения
## Не хватает прав для pull
- Если не хватает прав для pull с ошибкой: remote: `Write access to repository not granted. fatal: unable to access 'https://github.com/****/****.git/': The requested URL returned error: 403`, необходимо проверить выданные права на токен

---
## Передача прав на репозиторий другому пользователю

### ✅ **Что нужно сделать**

#### **1. Проверить текущий remote**
На локальной машине (или сервере):
```bash
git remote -v
```
Вы увидите что-то вроде:
```
origin  https://github.com/old_username/repo.git (fetch)
origin  https://github.com/old_username/repo.git (push)
```

---
#### **2. Обновить URL origin на новый**
Теперь поставьте URL с новым владельцем:
```bash
git remote set-url origin https://github.com/new_owner/repo.git
```
Проверьте:
```bash
git remote -v
```
Ожидаемый результат:
```
origin  https://github.com/new_owner/repo.git (fetch)
origin  https://github.com/new_owner/repo.git (push)
```

---
#### **3. Очистить старые креды (если использовали токен)**
Если вы на сервере использовали `git` через HTTPS, то мог сохраниться старый токен.
### Linux
Если авторизация была через credential helper:
```bash
git credential-cache exit
```
или очистить сохранённые креды:
```bash
git credential-cache --timeout=0
```
Если использовался менеджер GitHub CLI, то:
```bash
gh auth logout
gh auth login
```

---
#### **4. Подключиться заново с новым токеном**
Теперь выполните любую команду, требующую авторизации:
```bash
git pull
```
или
```bash
git push
```

Git запросит логин/пароль →  
вводите **логин нового владельца (или свой, если вас добавили collaborator) и токен**.
🔹 **Важно:** GitHub теперь принимает вместо пароля только токены.

---
### 🧩 Частый нюанс после передачи репозитория
Если новый владелец **переименовал репозиторий** — URL тоже меняется.  
Если репозиторий остался в той же организации/учётке — может быть другой формат URL.
Проверь правильный URL на GitHub → кнопка **Code → HTTPS**.

---
### ⭐ Итог
Достаточно выполнить:
1. `git remote set-url origin NEW_URL`
2. Очистить старые креды
3. Авторизоваться новым токеном
Локальный репозиторий не привязан к аккаунту — он просто хранит ссылку. Меняем ссылку и всё работает.
